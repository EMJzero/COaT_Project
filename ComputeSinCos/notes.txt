TAFFO:
The result is correct BUT...
The IR contains a fixed point version of sin and cos, however that is NOT the one actually used by main,
which instead uses a versions doing a mix of fixed point and floating point operations.
